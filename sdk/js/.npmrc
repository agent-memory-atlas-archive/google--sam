# The lockfile must resolve against the public registry: CI and every user
# install from it. A machine whose user config points npm at a mirror would
# otherwise write that mirror's URLs into package-lock.json.
registry=https://registry.npmjs.org/
